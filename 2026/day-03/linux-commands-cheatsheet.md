**Virtualization and VM**

\-> virtulization is like a magic to divde one room completly separate "mini apartments" inside the house.

\-> Each mini part is virtual machine, it has own door, furniture, and residents.

\-> if fire starts at one mini-apartment



**Hypervisor:**

\->  the software that create and runs VMs.



**Host OS:** the physical computer OS

**Guest Os:** install as an app on your os



ls -a                              → Show all files including hidden files

touch demo.txt                     → Create a new file

cat demo.txt                       → View file content

vim demo.txt                       → Edit file using vim editor

cat -n demo.txt                    → View file with line numbers



echo "hello" > demo.txt            → Overwrite file content

echo "hello" >> demo.txt           → Append content to file



grep "error" demo.txt              → Search word inside file

history                            → Show previously used commands

|                                  → Pipe output between commands



cp data demo                       → Copy file/directory

mv demo.txt demo-dosto.txt         → Rename or move file

rm demo.txt                        → Delete file

rm -r demo                         → Delete directory recursively

cp -r data demo                    → Copy directory recursively



find . -name "\*.txt"               → Find text files

head -5 demo.txt                   → Show first 5 lines of file



sudo apt update                    → Update package list

sudo apt install git               → Install git package

sudo apt remove git                → Remove git package

